# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Build / deps (extend per project)
node_modules/
bin/
obj/
dist/
*.log

# Local env / secrets -- never commit
.env
.env.*
*.local

# M365 Admin Tool: tenant list (secret-ish) + runtime data -- never commit
config.json
M365Snapshots/
M365AuditLog/
M365Logs/
M365Reports/
# Local docker-compose DATA_DIR volume (container runtime state)
/data/

# Session transfer / build-artifact staging (tarballs + handoff notes)
Handoff/

# Claude Code local session state (keep CLAUDE.md and .claude/commands/ tracked)
.claude/settings.local.json
